/**
 * The explanations behind the "?" buttons in the event editor.
 *
 * Kept out of the editor itself because they are prose, and prose mixed into JSX makes
 * both harder to read. Every number quoted here comes from `crates/detect/src/engine.rs`
 * — if the constants there change, these are wrong and there is nothing that will say so.
 */

/** The one that explains the whole scoring model. Sits next to the panel title. */
export const HOW_MATCHING_WORKS = (
  <>
    <p>
      Кожен рядок розпізнаної мови отримує <strong>бал</strong> для кожної події. Подія
      грає, якщо бал дійшов до її порогу — типово <code>0.82</code>.
    </p>
    <table>
      <tbody>
        <tr>
          <td>1.00</td>
          <td>Голосова команда — сказана навмисне, б’є все інше</td>
        </tr>
        <tr>
          <td>0.95</td>
          <td>Приклад фрази збігся точно</td>
        </tr>
        <tr>
          <td>0.88</td>
          <td>Приклад фрази збігся після відкидання закінчень</td>
        </tr>
        <tr>
          <td>0.85</td>
          <td>
            Ключове слово <em>плюс</em> слово дії — <strong>основний шлях</strong>
          </td>
        </tr>
        <tr>
          <td>0.55</td>
          <td>Саме лише ключове слово — нижче порога, не грає</td>
        </tr>
      </tbody>
    </table>
    <p>
      Ось чому «Меч лежить на столі» мовчить, а «Лицар махнув мечем» грає: об’єкт є в
      обох, дії — лише в другому.
    </p>
    <p className="hint__muted">
      Заперечення — це вето: якщо збіглося, подія не грає, хоч який був би бал.
    </p>
  </>
);

export const KEYWORDS_HINT = (
  <>
    <p>
      <strong>Що</strong> відбувається — іменники: <code>двері</code>, <code>меч</code>,{' '}
      <code>вовк</code>. Саме вони знаходять кандидатів.
    </p>
    <p>
      Слово саме по собі дає <code>0.55</code> — цього замало. Потрібне ще слово дії.
    </p>
    <p>
      <strong>Пастка:</strong> слова зводяться до кореня, але не всі. <code>меч</code> і{' '}
      <code>меча</code> — це один запис, а <code>меч</code> і <code>мечів</code> — два
      різні, бо корінь короткий. Так само <code>щит</code> і <code>щитом</code>. Якщо
      сумніваєшся — додай обидва, зайвий запис нічого не ламає.
    </p>
  </>
);

export const ACTIONS_HINT = (
  <>
    <p>
      <strong>Що з цим робиться</strong> — дієслова: <code>відчиняє</code>,{' '}
      <code>махнув</code>, <code>загарчав</code>. Разом із ключовим словом дають{' '}
      <code>0.85</code>.
    </p>
    <p>Саме по собі слово дії не створює кандидата — потрібні обидва.</p>
    <p>
      <strong>Пастка:</strong> префікс і вид міняють корінь. <code>гарчить</code> покриває{' '}
      <code>гарчиш</code> і <code>гарчимо</code>, але <em>не</em> покриває{' '}
      <code>загарчав</code>. Кожна префіксована й доконана форма — окремий запис.
    </p>
  </>
);

export const PHRASES_HINT = (
  <>
    <p>
      Цілі речення так, як ти їх кажеш. Не розбиваються на слова: збіг усього речення дає{' '}
      <code>0.95</code>, а після відкидання закінчень — <code>0.88</code>. Тобто фраза
      виграє в ключового слова, коли спрацювали обидва.
    </p>
    <p>
      Головне ж їхнє призначення — <strong>семантичне співставлення</strong>. Індекс
      значень будується лише з цих фраз, і саме він ловить те, що не має жодного спільного
      слова зі списками: «щось дрібне шаснуло під ногами» знаходить подію про дрібних
      істот.
    </p>
    <p className="hint__muted">
      Для цього потрібна необов’язкова модель зі вкладки «Налаштування». Без неї фрази
      працюють лише на точний і майже точний збіг.
    </p>
    <p>Це єдине поле без пасток: пиши речення як є, про корені думати не треба.</p>
  </>
);

export const NEGATIVES_HINT = (
  <>
    <p>
      <strong>Вето.</strong> Якщо будь-яка з цих фраз є в тексті, подія не грає — байдуже,
      який був бал.
    </p>
    <p>
      Тут живе те, що описує об’єкт, а не подію (<code>двері зачинені</code>), і те, що
      належить сусідній події (<code>з піхов</code> у «махнув мечем», щоб витягання клинка
      не рахувалось як удар).
    </p>
  </>
);

export const COMMANDS_HINT = (
  <>
    <p>
      Сказане навмисне, не в оповіді: «sound door». Бал <code>1.00</code> — б’є будь-яке
      автоматичне розпізнавання і не потребує ані ключового слова, ані дії.
    </p>
  </>
);

export const THRESHOLD_HINT = (
  <>
    <p>
      Бал, якого треба досягти, щоб подія зіграла. Вище — суворіше.
    </p>
    <p>
      <code>0.82</code> типово: ключове слово з дією (<code>0.85</code>) проходить, саме
      ключове слово (<code>0.55</code>) — ні. Піднімеш вище <code>0.85</code> — залишаться
      тільки точні збіги фраз.
    </p>
  </>
);

export const PROBABILITY_HINT = (
  <>
    <p>
      Шанс, що подія зіграє після того, як усе інше вже пройшло. <code>1.0</code> — завжди.
    </p>
    <p>
      Опусти нижче, якщо звук повторюється надто часто й починає звучати механічно: кроки,
      бій, дрібні істоти.
    </p>
  </>
);

export const COOLDOWN_HINT = (
  <>
    <p>Скільки чекати, перш ніж ця сама подія зможе зіграти ще раз.</p>
    <p>
      Захищає від довгого речення, у якому об’єкт згадано тричі. Одна фраза не спрацює
      двічі й поза межами затримки.
    </p>
  </>
);

export const REQUIRE_ACTION_HINT = (
  <>
    <p>
      Головний захист від хибних спрацювань. Поки увімкнено, саме лише ключове слово не
      грає: «Меч лежить на столі» мовчить.
    </p>
    <p className="hint__muted">
      Вимикай тільки для подій, у яких сама згадка об’єкта і є подією.
    </p>
  </>
);
